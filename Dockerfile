FROM johnnyutahio/meteor-launchpad:latest
# Currently broken by Debian version, change when https://github.com/jshimko/meteor-launchpad/issues/121 resolved
# FROM jshimko/meteor-launchpad:v2.3.0
# Dockerfile only exists to build a version with the code bundled for deployment.
# All the actual docker set-up is in https://github.com/jshimko/meteor-launchpad
