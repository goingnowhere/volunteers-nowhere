Components used to override behaviour of the accounts-ui module. Most likely we'll dump this and just interact with the Meteor accounts system directly instead of continuing to distort some outdated automagical library for doing something that's not that hard. - Rich
